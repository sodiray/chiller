---
title: "build"
group: "CLI"
---

The `build` command builds the next.js chiller app and produces two output directory in the current working directory: `.next` and `public`. You must run `chiller install` and `chiller sync` before you can build the local chiller app.

## Options

none

## Example

```
chiller build
```