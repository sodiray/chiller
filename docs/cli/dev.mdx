---
title: "dev"
group: "CLI"
---

The `dev` command starts the chiller app locally at `http://localhost:3000`. You must run `chiller install` and `chiller sync` before you can start the local chiller app.

## Options

none

## Example

```
chiller dev
```