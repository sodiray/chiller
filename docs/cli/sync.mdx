---
title: "sync"
group: "CLI"
---

The `sync` command copies all your files, per what your specified in the `chiller.json`, from your project into the `.chiller` app directory. You need to `sync` before running `build` or `dev`.

## Options

none

## Example

```
chiller sync
```